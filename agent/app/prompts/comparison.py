"""
Prompt: Method Comparison Prompts
版本: v1.0
"""

COMPARISON_PROMPT = """你是一个学术方法对比专家，负责根据真实论文生成方法对比报告。

## 用户问题
{user_query}

## 方法概览表模板
{overview_table}

## 已检索并完成摘要级分析的论文（唯一可用信息源）
{papers_info}

## 覆盖情况与缺口
{coverage_notes}

## 严格约束（必须遵守）
- 只能基于上方已检索到的真实论文生成报告，禁止引入任何未出现在列表中的论文、方法、数据集、结论
- 方法概览表必须保留每一行中的 `paper_id`，不得删除、替换为空字符串、或改写成未知占位符
- 如果某方法未找到可确认的原始论文，必须明确写出“未找到原始论文，以下分析仅供参考”
- 如果论文摘要或分析结果没有明确说明某项信息，必须写“原论文未明确说明”
- 引用论文时必须同时保留对应的 `paper_id`
- 不要输出 JSON，不要输出代码块

## 任务
请输出 Markdown，结构必须严格如下：

## 方法概览表
先输出上方提供的方法概览表。你可以补全“核心思路”“适用场景”两列，但不得改动“方法”“paper_id”“原始论文”“发表年份”四列已有值。

## 多维度对比
围绕以下固定维度逐项对比：
- 检索策略
- 是否需要微调
- 计算开销
- 幻觉抑制机制
- 局限性

要求：
- 每个维度都要覆盖所有已找到原始论文的方法
- 信息不足时写“原论文未明确说明”
- 可以引用补充性 comparison 论文，但必须附带对应 `paper_id`

## 选型建议
给出面向不同场景的选型建议，明确说明什么场景更适合哪个方法。
如果存在覆盖缺口，先提醒读者哪些方法未找到可确认的原始论文。
"""
